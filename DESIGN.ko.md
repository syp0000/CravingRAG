# CravingRAG — 설계 문서

> "상큼하고 과즙이 터지는 게 먹고 싶어" → 실제 레시피 추천 + 추천 이유

*English version: [DESIGN.md](DESIGN.md)*

---

## 1. 이 프로젝트가 푸는 문제

기존 레시피 검색은 **재료·요리명 키워드 매칭**이다. `WHERE ingredient IN ('chicken', 'rice')`.
하지만 사람이 실제로 먹고 싶은 걸 표현하는 방식은 그렇지 않다.

| 사용자가 실제로 하는 말 | 키워드 검색 | 필요한 것 |
|---|---|---|
| "상큼하고 과즙 터지는" | ❌ 매칭 불가 | 의미(임베딩) 검색 |
| "해장되는 뜨끈한 국물" | ❌ | 의미 검색 |
| "비 오는 날 어울리는" | ❌ | 의미 검색 |

**이 프로젝트의 존재 이유 = 감각적/감성적 쿼리는 벡터 검색으로만 풀린다.**
RAG를 배우기에 이보다 좋은 문제가 없다. 왜 임베딩이 필요한지를 몸으로 체감하게 되니까.

---

## 2. 핵심 설계 아이디어 (⭐ 가장 중요)

**나이브한 접근 (실패함):** 레시피 원문(재료 목록 + 조리법)을 그대로 임베딩한다.
→ `"2 cups flour, 1 tsp salt, bake at 350F"` 를 임베딩해봤자
   `"상큼하고 과즙 터지는"` 과 의미적으로 가깝지 않다. **검색이 안 먹힌다.**

**우리 접근 — 문서 강화(document enrichment):**
LLM으로 레시피마다 **감각 묘사(flavor profile)** 를 먼저 생성하고, **그걸** 임베딩한다.

```
원본 레시피
  title: "Citrus Summer Salad"
  ingredients: orange, grapefruit, mint, arugula, olive oil
        │
        │  ① CORTEX.COMPLETE — "맛·식감·어울리는 상황을 2문장으로"
        ▼
감각 묘사 (이게 진짜 검색 대상)
  "Bright citrus bursts with juice against crisp, peppery greens.
   Perfect for a hot summer lunch or as a palate cleanser after rich food."
        │
        │  ② AI_EMBED (다국어 모델)
        ▼
  VECTOR(FLOAT, 1024)
```

이제 `"상큼하고 과즙 터지는"` 을 임베딩하면 위 벡터와 **코사인 유사도가 높게** 나온다.

> 💡 이게 RAG에서 말하는 **"검색 가능한 형태로 문서를 다시 쓰기"** 다.
> 답변 시점이 아니라 **인덱싱 시점에 LLM을 쓴다**는 발상이 이 프로젝트의 기술적 핵심이고,
> 면접에서 설명할 포인트다.

### 읽는 사람이 아니라 임베딩을 위해 써라

이 프로젝트에서 품질이 가장 크게 뛴 건 묘사의 *정확도*가 아니라 **형식**을 바꿨을 때였다.

첫 버전은 생생한 2문장 묘사를 요구했고, 실제로 좋은 문장이 나왔다:

> "This dish is a harmonious blend of sweet and tangy flavors, with a subtle hint of
> saltiness. The texture is velvety and smooth..."

정확하고, 읽기 좋고, **거의 쓸모없었다.** 모든 프로필이 같은 뼈대로 나왔기 때문이다 —
*"This dish is a [X] blend of [Y] and [Z], with a subtle hint of [W]"*. 임베딩 입장에서 모든
문서가 비슷하게 생겼다. 공통 껍데기가 토큰의 대부분을 차지하면서 정보는 전혀 담지 않고,
그 껍데기가 벡터를 지배하는 동안 "과일 슬러시냐 튀긴 페이스트리냐"를 가르는 부분은 희석됐다.

증상은 점수에 그대로 드러났다. *"something refreshing and bursting with juice"* 의 상위 10개가
0.516~0.416 안에 몰려 있었고, 튀긴 비버테일·타코·핫워터크러스트 페이스트리가 섞여 나왔다.

프롬프트를 **간결하고 명사 밀도 높은** 형식으로 바꾸자 해결됐다:

> "Sorbet. Frozen fruit dessert. Sweet, tangy, refreshing. Smooth, icy, light.
> Fruit juice, fruit purée, honey."

같은 모델, 같은 코퍼스, 같은 임베딩 모델. **상위 10개 중 9개가 진짜 상큼한 요리로 바뀌었다.**

**직관에 반하는 교훈 두 가지:**

1. **여기서 문장력은 가치가 없고 오히려 해롭다.** 인덱싱된 문서를 읽는 사람은 없다.
   문법적 껍데기는 토큰만 먹고 검색 신호를 전혀 더하지 않는다.
2. **최고 유사도는 0.516 → 0.494로 *떨어졌는데* 결과는 훨씬 좋아졌다.**
   코사인 절대값은 직관과 다르게 동작한다 — 믿을 수 있는 건 순위뿐이다.
   절대값에 임계치를 거는 설계는 모래 위에 짓는 것이다.

`02_enrich.sql` 의 상투어 탐지 쿼리가 처음 보이는 것보다 중요한 이유도 이것이다.
문체 점검처럼 보였지만, 실은 **검색을 망가뜨리고 있던 바로 그것을 측정**하고 있었다.

### 이 방식이 만드는 리스크: 인덱싱 단계의 환각

문서 강화에는 일반 RAG에 없는 실패 모드가 있다. 먼저 명시해둔다.

**인덱싱 단계의 환각은 답변 단계의 환각보다 나쁘다. 눈에 보이지 않기 때문이다.**
틀린 *답변*은 출처를 보여주면 잡힌다. 틀린 *감각 묘사*는 벡터가 되어, 이후 모든 쿼리의
검색을 조용히 오염시키고, 그 원인이 된 문장을 아무도 보지 않는다.

게다가 **체계적으로 편향된다.** 묘사 품질은 모델에게 준 원본 정보량에 달려 있다:

| 소스 | LLM에 준 입력 | 결과 |
|---|---|---|
| Epicurious | 재료 전체 + 조리법 | 근거 있음 |
| worldcuisines | 180자 내외 설명 | 모델이 기억으로 빈칸을 채움 |

그리고 모델의 기억은 비서구 요리일수록 얕다 — 하필 크로스링구얼 쿼리가 노리는 그 요리들이다.
실제로 초기에 발견됨: 찐빵을 "earthy sweetness(흙 같은 단맛)"로 묘사했는데,
찐빵을 먹어본 사람은 아무도 그렇게 말하지 않는다.

`sql/02_enrich.sql` 에 넣은 완화책 두 가지:
1. 프롬프트의 **그라운딩 조항** — 모든 맛 주장을 제공된 텍스트에서 근거를 찾게 하고,
   정보가 얕으면 지어내는 대신 일반적으로 서술하게 한다.
2. **점검 쿼리** — 원본 텍스트와 생성된 묘사를 나란히 보여주고, 원본이 짧은 순으로 정렬.
   결국 도메인 지식만이 유일한 탐지기다. **자기가 제일 잘 아는 요리부터 확인할 것.**

정직한 트레이드오프: 그라운딩을 걸면 원본이 얕은 묘사가 밋밋해지고, 밋밋하면 검색이 나빠진다.
이 트레이드오프는 Phase 5에서 **소스별로 측정**해야지, 없는 셈 치면 안 된다.
진짜 해결책은 프롬프트를 더 조이는 게 아니라 **국제 요리에 더 많은 원본 정보를 주는 것**이다
(Phase 8의 REST API 소스).

---

## 3. 전체 데이터 흐름

```
 [HuggingFace] kaggle_food_recipes (13,501개 레시피)
        │  dlt — 증분 적재, 스키마 자동 추론, merge by recipe_id
        ▼
 RAW.RECIPES                      ← 원본 그대로
        │  SQL + CORTEX.COMPLETE   — 감각 묘사 생성 (배치)
        ▼
 ENRICHED.RECIPE_PROFILES         ← flavor_profile 텍스트
        │  SQL + AI_EMBED          — 다국어 임베딩
        ▼
 SEARCH.RECIPE_VECTORS            ← VECTOR(FLOAT, 1024)
        │
        │  사용자 쿼리 → AI_EMBED → VECTOR_COSINE_SIMILARITY
        ▼
 Top-K 레시피 (K=10)
        │  CORTEX.COMPLETE — 재순위 + "왜 이걸 추천했는지" 설명
        ▼
 Streamlit UI
```

---

## 4. 왜 Snowflake 안에서 다 하는가

| 단계 | 보통은 | 우리는 |
|---|---|---|
| 적재 | Airbyte / 커스텀 스크립트 | **dlt** → Snowflake |
| 임베딩 | OpenAI API (유료, 키 필요) | **Snowflake AI_EMBED** |
| 벡터 DB | Pinecone / Weaviate (별도 인프라) | **VECTOR 타입** (같은 테이블) |
| 생성 | OpenAI API | **CORTEX.COMPLETE** |

→ **외부 API 키 0개, 별도 벡터 DB 0개.** 데이터가 계정 밖으로 안 나가고,
무료 트라이얼 크레딧만으로 전체 RAG 파이프라인이 돌아간다.

---

## 5. 모델 선택 (중요한 결정)

레시피는 **영어**, 쿼리는 **영어 또는 한국어**. → 반드시 **다국어(multilingual) 임베딩 모델**.
영어 전용 모델을 쓰면 한국어 쿼리가 조용히 실패한다 (디버깅하기 어려운 흔한 실수).

| 용도 | 모델 | 차원 |
|---|---|---|
| 임베딩 | `snowflake-arctic-embed-l-v2.0` | 1024, **다국어** ✅ |
| (대안) | `voyage-multilingual-2` | 1024, 다국어 |
| ❌ 쓰지 말 것 | `snowflake-arctic-embed-m-v1.5` | 768, 영어 전용 |
| 생성 | `mistral-large2` 또는 `claude-3-5-sonnet` | — |

> **크로스링구얼 검색이 되는 이유:** 다국어 모델은 여러 언어를 *하나의 벡터 공간*에 매핑한다.
> "상큼한"과 "refreshing"이 벡터 공간에서 서로 가깝다. 번역 단계 없이 한국어 쿼리로
> 영어 레시피를 검색할 수 있다는 뜻이고, 이건 보여주기 좋은 특성이자 면접 소재다.

---

## 6. 단계별 구현 계획

### Phase 1 — 파이프라인 (RAG 아직 없음)
- [ ] Snowflake 트라이얼 계정 + DB/스키마/웨어하우스 (`sql/01_setup.sql`)
- [ ] dlt로 레시피 13,501개 적재 (`pipelines/load_recipes.py`)
- [ ] `SELECT COUNT(*)` 로 확인 — **여기까지가 순수 데이터 엔지니어링**

### Phase 2 — 인덱싱 (RAG의 절반)
- [ ] CORTEX.COMPLETE로 감각 묘사 생성 (`sql/02_enrich.sql`)
  - ⚠️ 처음엔 `LIMIT 20`! 13,501개 한 번에 돌리면 크레딧 낭비
- [ ] AI_EMBED로 벡터 생성 (`sql/03_embed.sql`)

### Phase 3 — 검색 (RAG의 나머지 절반)
- [ ] 코사인 유사도 Top-K 쿼리 (`sql/04_search.sql`)
- [ ] SQL에서 직접 쿼리를 넣고 결과를 눈으로 확인
  - **이 순간이 이 프로젝트의 하이라이트.** UI 없이 SQL만으로 RAG가 도는 걸 봄

### Phase 4 — 생성 + UI
- [ ] CORTEX.COMPLETE로 근거 기반 추천 이유 생성
- [ ] Streamlit 앱 (`app/streamlit_app.py`)

### Phase 5 — 평가 (⭐ 이력서 차별화 — 절대 건너뛰지 말 것)

포트폴리오 RAG 프로젝트 대부분이 Phase 4에서 멈춘다. 이 단계가
*"RAG 앱 만들었어요"* 와 *"검색 품질을 측정했고 설계를 방어할 수 있어요"* 를 가른다.

**비교할 3개 arm.** Arm A는 이 프로젝트의 핵심 주장 — 문서 강화가 효과가 있다 — 을
실제로 검증하기 위해 존재한다. 만약 A ≈ B 라면 핵심 아이디어가 틀린 것이고,
**그것도 정직하게 보고할 가치가 있는 결과다.**

| Arm | 임베딩 대상 | 검색 방식 | 답하는 질문 |
|---|---|---|---|
| **A** (베이스라인) | 재료 목록 원문 | 순수 벡터 | 문서 강화가 의미 있나? |
| **B** (우리 방식) | 감각 + 맥락 묘사 | 순수 벡터 | 강화가 얼마나 도움되나? |
| **C** (업그레이드) | 감각 + 맥락 묘사 | Cortex Search 하이브리드 | 키워드+벡터가 벡터 단독보다 나은가? |
| **D** (제거 실험) | 감각 묘사 **only** | 순수 벡터 | 추론된 맥락이 환각 리스크만큼의 값을 하나? |

Arm D가 있는 이유: 문서 강화는 성격이 다른 두 종류의 텍스트를 만든다. `sensory_profile` 은
원문에 근거하지만, `context_profile`(온도·계절·상황)은 대부분 모델의 자체 지식이고
**관측된 환각이 몰린 곳**이 바로 여기다. D와 B의 차이가 그 추론된 맥락의 값어치를 정확히 재준다 —
상황 쿼리("비 오는 날 어울리는")에는 도움이 되고 감각 쿼리에는 영향이 없어야 정상이다.
전체적으로 D ≈ B 라면, 위험한 절반이 값을 못 하고 있는 것이다.

**평가셋 만들기** (`eval/queries.yml`)

RAG 평가의 어려운 부분은 **정답(ground truth)** 이다. 쿼리마다 "어떤 레시피가 나왔어야
하는지"를 알아야 한다.

1. **고정된 부분집합**을 정한다 (예: 강화된 레시피 200개). 13,501개 전체를 손으로 라벨링하는 건
   비현실적이고, **작지만 완전히 라벨링된 코퍼스가 크지만 부분적으로 라벨링된 것보다 신뢰도가 높다.**
2. 쿼리 20개를 카테고리별로 작성: 감각 / 상황 / 제약조건 / 크로스링구얼.
3. 쿼리별로 관련 레시피를 라벨링한다. 속도를 위해 LLM으로 후보를 1차 선별하고 사람이 검증하되,
   **README에 "라벨링에 LLM을 사용했다"고 명시**할 것 — LLM이 유사하다고 보는 쪽으로 편향이 생기므로.

**지표**

```
Recall@5 = (상위 5개 안에 들어온 관련 레시피 수) / min(5, 해당 쿼리의 전체 관련 레시피 수)
```

arm별로 전체 쿼리 평균을 보고한다. 그리고 **실패 케이스**도 기록한다 — recall이 가장 나쁜
3~5개 쿼리와 그 원인을 한 줄씩. **자기 시스템이 어디서 깨지는지 설명할 수 있는 지원자는
성공 사례만 보여주는 지원자보다 훨씬 시니어로 읽힌다.**

**같이 기록하면 좋은 것:** 인덱싱에 쓴 크레딧, 쿼리당 비용. 비용은 데이터 팀이 매일 하는
대화인데 포트폴리오에서 이걸 보고하는 경우가 거의 없다.

### Phase 6 — 제약 조건 검색 (선택, 가치 높음)

보유 재료 `PANTRY` 테이블을 추가하고, *"상큼한 거 **근데 오늘 밤에 실제로 만들 수 있는 걸로**"*
에 답한다. 생각보다 흥미로운데, 순수 의미 검색이 **필터링된 벡터 검색**으로 바뀌면서
진짜 엔지니어링 트레이드오프가 생기기 때문이다.

| 전략 | 방식 | 실패 모드 |
|---|---|---|
| **Post-filter** | 유사도 상위 K개 뽑고 → 못 만드는 것 제거 | K개가 전부 제거되면 **결과가 0개** |
| **Pre-filter** | 만들 수 있는 것만 남기고 → 유사도 정렬 | 정확하지만 훨씬 많은 행을 스캔 |

둘 다 구현하고 recall과 지연시간을 측정한 뒤, 언제 어느 쪽을 택할지 설명한다.
이 트레이드오프는 실무 벡터 검색에서 잘 알려진 문제인데 포트폴리오에는 거의 안 나온다.

> 참고: 팬트리는 **손으로 쓴 테이블이어도 된다. 영수증 스캔이 전혀 필요 없다** —
> 흥미로운 엔지니어링은 필터링이지 데이터 입력이 아니다.

### Phase 7 — 영수증 적재 (선택, 제일 마지막)

영수증 사진 → OCR / 멀티모달 LLM → 품목 정규화 → `PANTRY` 테이블.

진짜 멀티모달 요소가 추가되고, 형태와 주기가 다른 **두 번째 dlt 소스**가 생겨서 파이프라인
스토리가 강해진다. 하지만 가장 지루한 작업이기도 하다 (실제 영수증은 지저분하고
`"ORG BAN 3LB"` → `banana` 같은 품목 정규화가 필요하다). 그리고 **검색 품질에 대한
인사이트는 전혀 주지 않는다.** Phase 5와 6이 끝난 뒤에만 만들 것.

### Phase 8 — 실시간 REST API 소스 (여기서 제일 전이 가능한 기술)

현재 두 소스는 모두 한 번 받아온 정적 CSV다. 진짜 인제스션 파이프라인은 페이지네이션,
레이트 리밋, 증분 로딩이 있는 **살아있는 API**에서 가져온다. dlt의 `rest_api` 소스가
바로 그걸 위한 기능이고, 이 프로젝트에서 **데이터 엔지니어링 직무로 가장 잘 전이되는 부분**이다.

좋은 대상: **TheMealDB** — 무료 공개 API, 약 195개 지역/요리, 요리별 재료 포함.
Wikipedia / Wikidata API도 괜찮다.

이건 "진짜 같아 보여서"가 아니다. 위의 인덱싱 환각 문제에 대한 **실제 해결책**이다.
지금 국제 요리는 짧은 설명만 갖고 있어서 모델이 지어낸다. 요리별 실제 재료를 주는 API가 있으면
문서 강화가 근거를 갖게 되는데, 이건 프롬프트를 아무리 조여도 대체할 수 없다.

> **HTML 스크래핑은 하지 말 것.** 대부분의 레시피 사이트가 약관에서 스크래핑을 금지하고,
> 약관 위반 스크래퍼가 포트폴리오 레포에 있으면 하이어링 매니저에게 적극성이 아니라
> **판단력 문제**로 읽힌다. 공개 API와 사이트가 스스로 게시하는 `schema.org/Recipe` JSON-LD는
> 소비되라고 만든 것이니 그걸 쓰면 된다.

---

## 7. 발견

아래는 전부 이 코퍼스에서 관측됐고, 명시된 경우 통제 실험으로 재현했다.
세 그룹으로 나뉘는데, **그 분류 자체가 핵심이다: 대부분의 실패는 설계 실수였고,
일부는 기술의 속성이며, 둘을 구분하는 것이 별개의 능력이다.**

### A. 인덱싱 방식이 만든 실패 — 전부 고칠 수 있음

| 발견 | 증거 | 해결 |
|---|---|---|
| 프로즈 형식이 검색을 파괴 | 상위 10개가 0.516–0.416에 압축, 튀긴 페이스트리가 과일 슬러시와 동급 | 간결한 명사 위주 → 9/10 적합 |
| 요리의 정체성이 인덱스에 있어야 함 | "맛과 식감만 묘사"가 *soup* / *broth* 를 지워서 칼국수가 "warm broth"에 안 걸림 | 요리 종류를 먼저 명시 |
| 원문이 얕으면 강화가 환각을 만듦 | 찐빵을 "earthy"로, 속 채운 멜론을 "cool"인데 "warm stuffing"이라고 | 그라운딩 조항 + 80자 미만 제외 |
| 일반적인 프로필은 허브가 됨 | "Nice biscuit"이 20개 쿼리 중 6개에 등장, 그중 둘은 서로 모순 | 간결 형식이 허브를 200개 중 2개로 감소 |

### B. 벡터 검색에 내재한 실패 — 프롬프트로 못 고침

**순위가 어휘 일치로 붕괴한다.** *"something refreshing and bursting with juice"* 에서
후보 12개가 프로필에 **"refreshing"이라는 단어를 포함하는지**로 완벽하게 갈렸다 —
순위 1–7은 전부 포함, 8–12는 전부 미포함. "juicy" 포함 여부는 순위를 전혀 예측하지 못했다.
프로필에 문자 그대로 *"Juicy, crunchy, creamy"* 라고 적힌 토마토 샐러드가 마가리타들에게 졌다.

**재료와 식감을 구분하지 못한다.** 재료에 *"lime juice"* 가 있는 마가리타가
*"juicy"* 로 묘사된 토마토 샐러드보다 높다. 쿼리에서 "refreshing"을 빼도 뒤집히지 않았다 —
음료가 "juice"에도 어휘적으로 일치하기 때문이다.

**코퍼스에 없는 어휘는 완전히 실패한다.** *"firm food that squirts liquid when you bite
into it"* 은 워터 비스킷, 브레드스틱, 다이제스티브를 반환했다 — 요청의 정반대인 마른 과자들.
어느 프로필에도 "squirts"가 없어서 모델이 "firm"에만 매달린 것이다.
개념은 코퍼스에 있지만("juicy") 표현이 없다.

**부정은 표현되지 않는다.** *"vegetarian, no meat at all"* 이 햄버거와 케밥을 반환한다.
*"no oven required"* 는 수플레와 프레첼을 반환하는데, "No-Knead Pizza Dough" 같은 제목의
**"No-" 라는 글자**에 반응한 것이지 개념이 아니다.

**관용 표현은 지시대상을 잃는다.** *"crispy on the outside, tender inside"* 는 관용적으로
튀기거나 구운 육류를 가리키지만 크루아상과 Nice biscuit이 나왔다.
임베딩은 단어의 의미는 잡아도 그 표현이 무엇을 가리키는지는 못 잡는다.

### C. 정직하게 측정하는 법에 대한 발견

**검색을 탓하기 전에 항상 코퍼스를 확인하라.** 해장 국물 한국어 쿼리가 빈달루와 차가운
슬로를 반환했고, 이는 크로스링구얼 검색이 망가진 것처럼 보였다. 아니었다 —
세계 요리의 14%만 강화돼서 한국 국물 15개 중 약 2개만 벡터로 존재했다. **찾을 게 없었던 것이다.**
이 실수를 두 번 하고 나서야 커버리지 확인이 습관이 됐다.

**절대 유사도는 해석 불가능하고, 순위만 믿을 수 있다.** 간결 프로필은 1위 점수가
*더 낮았는데도*(0.494 vs 0.516) 결과는 훨씬 좋았다. 이 모델에서 무관한 텍스트가
약 0.495에서 바닥을 치므로 "0.5 미만이면 매치 없음" 같은 임계치는 전부를 걸러낸다.
다만 **고정된 인덱스 안에서는** 1위 점수가 품질을 따라가는 듯하다 —
잘 답한 쿼리는 0.606, 못 답한 쿼리는 0.460. 어디까지나 인덱스 내부 신호다.

**맞는 답이 틀린 메커니즘에서 나올 수 있다.** "Fish ball"이 squirts 쿼리에서 1위였고
사실 괜찮은 답이다 — 어묵은 국물을 머금는다. 그런데 프로필엔 *"Chewy, firm"* 뿐이다.
시스템은 "firm"에 매칭해서 운 좋게 맞은 것이다. Recall은 이걸 정답으로 세므로
**지표가 시스템을 실제보다 좋게 보이게 한다.**

**정답이 하나가 아닌 쿼리가 있고, 이것이 달성 가능한 recall의 상한을 정한다.**
*"상큼하고 과즙이 터지는"* 에 대해 사람마다 다른 답을 낼 것이다 — 방울토마토, 수박 샐러드,
자몽, 아구아프레스카 모두 방어 가능하다. 평가자 간 일치도를 재지 않으면 Recall@5 65%라는
숫자를 해석할 수 없다. 상한이 100%인지 75%인지 모르니까.
일부를 두 번 판정하는 것이 값싼 해결책이고, 다음 단계로 명시해둔다.

---

## 8. 이력서 한 줄

> **CravingRAG** — Snowflake Cortex 기반 크로스링구얼 RAG 검색 파이프라인.
> dlt로 1만3천 건 레시피를 적재하고, LLM 문서 강화로 감각 묘사를 생성해 임베딩함으로써
> "상큼하고 과즙 터지는" 같은 감성 자연어 쿼리를 영어 레시피 코퍼스에 매칭.
> 순수 벡터 검색 대비 하이브리드 검색의 Recall@5를 벤치마크.

---

## 9. 의도적으로 안 만드는 것

이전 프로젝트(PantryAI)에서 이미 증명했고, 초점을 흐리므로 **제외**:
- ❌ 유저 인증 / 소셜 기능 / meal planner
- ❌ 레시피 *생성* — 이 프로젝트는 실제 레시피를 **검색**한다.
  지어내는 건 증명하려는 것의 정반대다

팬트리 재고는 **제외가 아니다.** 다만 재고 *관리 기능*이 아니라 **검색 제약 조건**(Phase 6)으로
들어온다. 이 구분이 중요하다 — 가치는 필터링된 벡터 검색에 있지, 팬트리 목록 CRUD에 있지 않다.

이 프로젝트는 **데이터 레이어 + 검색 품질**에 집중한다.
모든 단계는 검색을 개선하거나, 검색을 측정해야 한다.
